import { Outlet } from "react-router-dom";
import Categories from "../components/categories.component";

const Home = () => {
  return (
    <div>
      <Categories />;
    </div>
  );
};

export default Home;
